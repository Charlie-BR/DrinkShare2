<!DOCTYPE html>
<html>
  <head>

    <meta charset="UTF-8">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css" rel="stylesheet" />
    <script src="//code.jquery.com/jquery-1.11.0.min.js"></script>
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.2/js/bootstrap.min.js"></script>


  </head>
  
  <body>
    <%for(var i = 0; i < title.length; i ++) {%>
      <h4><%=title[i].name%></h4>
      <% } %>
  	<div class='container'>
	  	<header>
        Data Share | <a href="#">Profile</a>
        <img id='logo' src="http://cdn.liquidplanner.com/wp-content/uploads/5701090.jpg">
	  	</header>

	  	<div id='search'>
	  		<form id="form"method="post" action="/users/test">
          <input id="input-bar" class='search-bar' type='text' placeholder='Search Bar'>
          <input id="submit-bar" class='search-bar' type='submit'>
        </form>
	  	</div>

      <button id="list-bars">List all da bars</button>
      <% if (name) {%>
        <h2 id="name1">The bar is <%=name%>, the wait is <%=avgWaitTime%> minutes</h1>
      <% }%>
      <h2 id="name2"></h2>
	  	<div id ='data-results'> 
	  	</div>

	  	<div id='map'>
	  		Map goes here
	  	</div>
  	</div>

    <div class="btn btn-danger" href="#server_msg_modal" data-toggle="modal">Add New Wait Time</div>

      <div class="modal fade" id="server_msg_modal">
        <div class="modal-dialog">
          <div class="modal-content">
            <div class="modal-header">
              <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
              <h4 class="modal-title">How long did it take?</h4>
            </div>
            <div class="modal-body">
              <div class="btn-group btn-group-justified" role="group" aria-label="...">
                <div class="btn-group" role="group">
                  <button type="button" class="btn btn-default">Beer</button>
                </div>
                <div class="btn-group" role="group">
                  <button type="button" class="btn btn-default">Wine</button>
                </div>
                <div class="btn-group" role="group">
                  <button type="button" class="btn btn-default">Well Liquor</button>
                </div>
                <div class="btn-group" role="group">
                  <button type="button" class="btn btn-default">Fancy Cocktail</button>
                </div>
                  
              </div> 
              <input type="number" name="input" value="How Many Minutes did you Wait?">           
            </div>
          </div><!-- /.modal-content -->
        </div><!-- /.modal-dialog -->
      </div><!-- /.modal -->

      <script>
      $(function() {
        $("#list-bars").click(function(){
          var stringy = "";
          <%for(var i = 0; i < title.length; i ++) {%>
            stringy += "<%=title[i].name%> "
            $("#name2").html(stringy);
          <% } %>
        });

        $("#submit-bar").click(function(){
          var params = $("#input-bar").val();
          $("#form").attr("action","/users/test/" + params)
        });
      });
      </script>
  </body> 
</html>